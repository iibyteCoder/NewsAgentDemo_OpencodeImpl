# 智能体提示词最佳实践

## 核心原则

1. **简洁** - 控制在 100-200 行
2. **清晰** - 明确输入输出和职责边界
3. **引用** - 使用模板避免重复定义
4. **意图** - 描述"做什么"而非"怎么做"

---

## 编写规则

### 1. Frontmatter（必需）

```yaml
---
description: 一句话描述
mode: subagent | main
temperature: 0.1 | 0.2
maxSteps: 数字
hidden: true | false
---
```

### 2. 核心职责（3-7项）

用数字列表，每项一句话描述：

```markdown
## 核心职责

1. **职责1** - 简短描述
2. **职责2** - 简短描述
3. **职责3** - 简短描述
```

### 3. 输入输出

清晰定义参数和返回格式：

```markdown
## 输入参数

从 prompt 中提取：

- `param1`: 说明
- `param2`: 说明

## 输出要求

返回 JSON 包含：

- `field1`: 说明
- `field2`: 说明
```

### 4. 工作流程

简洁的步骤列表，每个步骤一句话：

```markdown
## 工作流程

### 1. 步骤名称

简短说明。

**关键点**：

- 要点1
- 要点2

### 2. 下一步骤

...
```

**设计原则**：

- **目的导向**：描述"做什么"，让智能体决定"怎么做"
- **信任判断**：智能体能根据目标选择合适的工具和参数
- **避免过度约束**：明确的最佳实践直接给出，无需对比或解释

反面示例：
- ❌ 指定具体工具参数、搜索关键词组合、详细操作步骤
- ❌ 对比多种方式并标注推荐/不推荐
- ❌ 解释为什么这样做（原因、好处等）

### 5. 引用模板

使用 `@templates/xxx.md` 引用，避免重复定义结构：

```markdown
## 报告格式

参考模板：`@templates/sections/validation-section-template.md`

### 填充规则

- 填充格式说明
- 关键要求
```

**不要这样做**：

```markdown
## 报告格式

```markdown
# 事件名称

**时间**：{timestamp}
...
```

```

### 6. 避免详细命令

用自然语言描述检查项和修正要求，避免具体 shell 命令：

❌ **不好**：

```markdown
| 检查项 | 命令/方法 | 需修正的问题 |
|--------|----------|------------|
| 模板残留 | `grep -i "填充格式说明\|占位符说明"` | 各种说明性章节 |
| 空占位符 | `grep -E '\{[a-zA-Z_]+\}'` | 未替换的占位符 |
```

✅ **好**：

```markdown
**检查项**：

1. **模板残留** - 检查是否存在"填充格式说明"、"占位符说明"等说明性章节
2. **空占位符** - 检查是否存在 `{event_summary}`, `{news_content}` 等未被替换的占位符
3. **重复标题** - 检查是否存在相同的H2或H3标题
```

### 7. 可用工具

简短说明：

```markdown
## 可用工具

- `tool_name` - 简短说明
- `@agent_name` - 简短说明
```

### 8. 关键原则

最重要的规则，用优先级标记：

```markdown
## 关键原则

1. ⭐⭐⭐ **最重要** - 说明
2. ⭐⭐ **重要** - 说明
3. ⭐ **一般** - 说明
```

### 9. 错误处理

简洁的箭头格式：

```markdown
## 错误处理

- 场景1 → 处理方式1
- 场景2 → 处理方式2
- 场景3 → 处理方式3
```

---

## 项目特定规则

### Session ID 管理

只有 `coordinator.md` 可以生成 session_id，其他智能体必须从参数获取：

```markdown
1. ⭐⭐⭐ **session_id 管理** - 从 prompt 参数获取，禁止自己生成
```

### 并行执行

多个独立任务必须同时启动：

```markdown
### 3. 并行启动分析任务

同时启动三个任务（并行执行）：

- @agent1
- @agent2
- @agent3
```

### 数据验证

每个关键步骤后必须检查数据：

```markdown
### 2. 数据检查

**搜索后验证**：

- 有结果 → 继续
- 无结果 → 返回 no_data，终止任务
```

---

## 检查清单

### 结构

- [ ] 包含 frontmatter
- [ ] 包含核心职责（3-7项）
- [ ] 包含输入输出
- [ ] 包含工作流程
- [ ] 包含可用工具
- [ ] 包含关键原则
- [ ] 包含错误处理

### 内容

- [ ] 总行数 < 200 行
- [ ] 没有冗长的代码示例
- [ ] 使用模板引用而非重复定义
- [ ] session_id 管理规则正确（如适用）
- [ ] 并行执行要求明确（如适用）
- [ ] 数据验证步骤完整

---

## 常见错误

### ❌ 过度解释

```markdown
## 关于搜索功能的详细说明

搜索是我们系统的核心功能之一。当用户请求搜索时，我们需要...
（300字的详细说明）
```

### ✅ 简洁明了

```markdown
### 1. 搜索新闻

使用 `web-browser_multi_search_tool` 搜索：

- num_results: 5-10
- search_type: "news"
```

### ❌ 重复定义结构

在多个提示词中重复相同的报告格式

### ✅ 引用模板

```markdown
参考模板：`@templates/sections/validation-section-template.md`
```

### ❌ 提供完整代码

```markdown
## Python 实现示例

```python
def extract_filename(path):
    # 50行完整实现
```
```

### ✅ 说明意图

```markdown
### 修正图片路径

- 从路径中提取文件名
- 改为相对路径：`./{event_name}/{filename}`
```
