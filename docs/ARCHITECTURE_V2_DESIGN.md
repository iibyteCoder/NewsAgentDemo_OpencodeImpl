# NewsAgent 新架构设计文档

## 版本信息

- **版本**: v2.0
- **创建日期**: 2026-01-31
- **作者**: NewsAgent 团队
- **状态**: 设计中

---

## 目录

1. [架构概述](#架构概述)
2. [完整流程图](#完整流程图)
3. [分层架构详解](#分层架构详解)
4. [智能体职责矩阵](#智能体职责矩阵)
5. [执行时序](#执行时序)
6. [数据流依赖](#数据流依赖)
7. [需要新增的智能体](#需要新增的智能体)
8. [需要修改的文件](#需要修改的文件)
9. [错误处理机制](#错误处理机制)
10. [实施计划](#实施计划)

---

## 架构概述

### 设计原则

1. **三层架构**：分析层 → 内容层 → 组装层
2. **两层处理**：数据收集层（Builder）→ 文件生成层（Generator）
3. **串并行结合**：最大化并行效率，保证数据依赖正确
4. **容错机制**：出错不影响整体流程

### 核心改进

- ✅ 解决图片部分缺失问题
- ✅ 明确数据流依赖关系
- ✅ 最大化并行执行效率
- ✅ 保持两层架构的清晰职责

---

## 完整流程图

```
═══════════════════════════════════════════════════════════════════════════
                        单个事件处理完整流程
═══════════════════════════════════════════════════════════════════════════

开始
  │
  ↓
┌─────────────────────────────────────────────────────────────────┐
│                    @event-processor                              │
│                   事件处理器（统一入口）                           │
└─────────────────────────────────────────────────────────────────┘
  │
  ├── 读取事件新闻数据 (news-storage_search)
  │
  └── 启动【第一层：分析层】
      │
      ↓
═══════════════════════════════════════════════════════════════════════════
                        【第一层：分析层】
                      ✅ 并行执行（同时启动）
═══════════════════════════════════════════════════════════════════════════

  ┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
  │   任务1：验证流程      │  │   任务2：时间轴流程    │  │   任务3：预测流程      │
  │  同时启动              │  │  同时启动              │  │  同时启动              │
  └───────────────────────┘  └───────────────────────┘  └───────────────────────┘
           │                           │                           │
           ↓                           ↓                           ↓
┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
│  @validator-builder    │  │ @timeline-builder      │  │  @predictor-builder    │
│  (验证数据收集器)       │  │ (时间轴数据收集器)     │  │ (预测数据收集器)       │
├───────────────────────┤  ├───────────────────────┤  ├───────────────────────┤
│ 1. 搜索验证来源        │  │ 1. 搜索历史事件        │  │ 1. 搜索专家观点        │
│ 2. 并行处理搜索结果    │  │ 2. 并行处理搜索结果    │  │ 2. 并行处理搜索结果    │
│ 3. 生成验证数据        │  │ 3. 生成时间轴数据      │  │ 3. 生成预测数据        │
│ 4. 保存到数据库        │  │ 4. 保存到数据库        │  │ 4. 保存到数据库        │
│   section_type=        │  │   section_type=        │  │   section_type=        │
│   "validation"         │  │   "timeline"           │  │   "prediction"        │
└───────────────────────┘  └───────────────────────┘  └───────────────────────┘
           │                           │                           │
           ↓                           ↓                           ↓
┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
│@validation-report-    │  │@timeline-report-       │  │@prediction-report-     │
│ generator             │  │ generator              │  │ generator              │
│ (验证报告生成器)       │  │ (时间轴报告生成器)     │  │ (预测报告生成器)       │
├───────────────────────┤  ├───────────────────────┤  ├───────────────────────┤
│ 1. 读取数据库         │  │ 1. 读取数据库         │  │ 1. 读取数据库         │
│    section_type=      │  │    section_type=      │  │    section_type=      │
│    "validation"       │  │    "timeline"         │  │    "prediction"       │
│ 2. 填充模板           │  │ 2. 填充模板           │  │ 2. 填充模板           │
│ 3. 写入临时文件       │  │ 3. 写入临时文件       │  │ 3. 写入临时文件       │
│    .parts/03-         │  │    .parts/04-         │  │    .parts/05-         │
│    validation.md      │  │    timeline.md        │  │    prediction.md      │
└───────────────────────┘  └───────────────────────┘  └───────────────────────┘
           │                           │                           │
           └─────────────────┬─────────┴───────────────────────────┘
                             │
                             ↓
                    【等待分析层全部完成】
                             │
                             ↓
═══════════════════════════════════════════════════════════════════════════
                        【第二层：内容层】
          步骤1串行（新闻）→ 步骤2并行（总结 + 图片）
═══════════════════════════════════════════════════════════════════════════

  ┌────────────────────────────────────────────────────────────────┐
  │ 步骤1：新闻列表流程（串行）                                     │
  └────────────────────────────────────────────────────────────────┘
                         │
                         ↓
┌────────────────────────────────────────────────────────────────┐
│  @news-builder                                               │
│  (新闻数据收集器)                                               │
├────────────────────────────────────────────────────────────────┤
│ 1. 从 news 表读取事件新闻                                       │
│ 2. 分类为今日新闻/相关新闻                                      │
│ 3. 保存到数据库                                                │
│    section_type="news"                                         │
└────────────────────────────────────────────────────────────────┘
                         │
                         ↓
┌────────────────────────────────────────────────────────────────┐
│  @news-report-generator                                       │
│  (新闻报告生成器)                                               │
├────────────────────────────────────────────────────────────────┤
│ 1. 读取数据库 section_type="news"                             │
│ 2. 填充模板                                                   │
│ 3. 写入临时文件 .parts/02-news.md                              │
└────────────────────────────────────────────────────────────────┘
                         │
                         ↓ 【完成步骤1，开始步骤2】

  ┌────────────────────────────────────────────────────────────────┐
  │ 步骤2：总结 + 图片（并行）                                     │
  └────────────────────────────────────────────────────────────────┘
                         │
           ┌─────────────┴─────────────┐
           │                           │
           ↓ 并行启动                   ↓ 并行启动
┌─────────────────────┐     ┌─────────────────────┐
│ 任务A：摘要流程      │     │ 任务B：图片流程      │
└─────────────────────┘     └─────────────────────┘
           │                           │
           ↓                           ↓
┌─────────────────────┐     ┌─────────────────────┐
│ @summary-builder ⭐ │     │ @images-builder ⭐  │
│ (摘要数据收集器)      │     │ (图片数据收集器)      │
├─────────────────────┤     ├─────────────────────┤
│ 1. 从 news 表读取    │     │ 1. 读取数据库        │
│    事件新闻          │     │    section_type=    │
│ 2. 分析新闻内容      │     │    "news" ⚠️ 依赖步骤1│
│ 3. 生成2-3句话摘要   │     │ 2. 提取 image_urls  │
│ 4. 保存到数据库      │     │ 3. 创建图片文件夹    │
│    section_type=     │     │ 4. 下载图片到本地    │
│    "summary"         │     │ 5. 生成图片元数据    │
└─────────────────────┘     │ 6. 保存到数据库      │
           │                 │    section_type=     │
           ↓                 │    "images"         │
┌─────────────────────┐     └─────────────────────┘
│ @summary-report-     │               │
│ generator           │               │
├─────────────────────┤               │
│ 1. 读取数据库        │               │
│    section_type=     │               │
│    "summary"         │               │
│ 2. 填充模板          │               │
│ 3. 写入临时文件      │               │
│    .parts/01-        │               │
│    summary.md        │               │
└─────────────────────┘               │
           │                           │
           └─────────────┬─────────────┘
                         │
                         ↓
                    【内容层全部完成】
                         │
                         ↓
═══════════════════════════════════════════════════════════════════════════
                        【第三层：组装层】
═══════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────┐
│  @report-assembler                                             │
│  (报告组装器)                                                   │
├────────────────────────────────────────────────────────────────┤
│ 1. 收集 .parts 文件夹中的所有 md 文件                          │
│ 2. 检查哪些部分成功生成                                        │
│ 3. 按顺序合并：                                                │
│    01-summary.md                                               │
│    02-news.md                                                  │
│    03-validation.md                                            │
│    04-timeline.md                                              │
│    05-prediction.md                                             │
│    06-images.md                                                │
│ 4. 修正图片链接（使用相对路径）                                │
│ 5. 添加分隔线                                                  │
│ 6. 生成最终报告 {event_name}.md                                │
│ 7. 可选：清理 .parts 临时文件夹                                │
└────────────────────────────────────────────────────────────────┘
                         │
                         ↓
                      完成
```

---

## 分层架构详解

### 第一层：分析层（并行执行）

**特点**：无数据依赖，可完全并行

| 任务   | 数据收集层         | 保存字段                  | 文件生成层                   | 输出文件                |
| ------ | ------------------ | ------------------------- | ---------------------------- | ----------------------- |
| 验证   | @validator-builder | section_type="validation" | @validation-report-generator | .parts/03-validation.md |
| 时间轴 | @timeline-builder  | section_type="timeline"   | @timeline-report-generator   | .parts/04-timeline.md   |
| 预测   | @predictor-builder | section_type="prediction" | @prediction-report-generator | .parts/05-prediction.md |

**执行方式**：

```python
# 三个任务同时启动，互不干扰
Task(@validator-builder) → @validation-report-generator
Task(@timeline-builder) → @timeline-report-generator
Task(@predictor-builder) → @prediction-report-generator
```

---

### 第二层：内容层（步骤1串行 → 步骤2并行）

**特点**：

- 步骤1（新闻）：独立执行，无依赖
- 步骤2（总结+图片）：并行执行，都依赖步骤1

| 阶段  | 任务 | 数据收集层       | 依赖            | 文件生成层                | 输出文件             |
| ----- | ---- | ---------------- | --------------- | ------------------------- | -------------------- |
| 步骤1 | 新闻 | @news-builder  | news表          | @news-report-generator    | .parts/02-news.md    |
| 步骤2 | 摘要 | @summary-builder | news表          | @summary-report-generator | .parts/01-summary.md |
| 步骤2 | 图片 | @images-builder  | 步骤1的news数据 | @images-report-generator  | .parts/06-images.md  |

**执行方式**：

```python
# 步骤1：新闻流程
@news-builder → @news-report-generator
[等待完成]
↓
# 步骤2：摘要 + 图片 并行执行
Task(@summary-builder → @summary-report-generator)
Task(@images-builder → @images-report-generator)
```

---

### 第三层：组装层

**职责**：收集所有临时文件，组装为最终报告

**输入**：`.parts/` 文件夹中的 md 文件

**输出**：`{event_name}.md`

---

## 智能体职责矩阵

### 现有智能体（需修改）

| 智能体                       | 当前状态 | 需要修改              |
| ---------------------------- | -------- | --------------------- |
| @event-processor             | ✅ 存在  | 🔧 修改为三层架构调用 |
| @validator-builder           | ✅ 存在  | ✅ 保持不变           |
| @timeline-builder            | ✅ 存在  | ✅ 保持不变           |
| @predictor-builder           | ✅ 存在  | ✅ 保持不变           |
| @validation-report-generator | ✅ 存在  | ✅ 保持不变           |
| @timeline-report-generator   | ✅ 存在  | ✅ 保持不变           |
| @prediction-report-generator | ✅ 存在  | ✅ 保持不变           |
| @news-builder              | ✅ 存在  | ✅ 保持不变           |
| @news-report-generator       | ✅ 存在  | ✅ 保持不变           |
| @summary-report-generator    | ✅ 存在  | ✅ 保持不变           |
| @images-report-generator     | ✅ 存在  | 🔧 修改为从数据库读取 |
| @report-assembler            | ✅ 存在  | 🔧 简化为纯文件组装   |

### 新增智能体

| 智能体           | 职责                 | 优先级 |
| ---------------- | -------------------- | ------ |
| @summary-builder | 生成摘要数据         | P0     |
| @images-builder  | 下载图片并生成元数据 | P0     |

---

## 执行时序

### 时间轴

```
T0: @event-processor 启动
    ├─ 读取事件新闻数据
    └─ 启动第一层

T1-T10: 【第一层：分析层】并行执行
    ├─ @validator-builder → @validation-report-generator
    ├─ @timeline-builder → @timeline-report-generator
    └─ @predictor-builder → @prediction-report-generator

T11: 【等待分析层完成】

T12: 【第二层：内容层】步骤1
    └─ @news-builder → @news-report-generator

T13: 【等待步骤1完成】

T14-T15: 【第二层：内容层】步骤2 并行执行
    ├─ @summary-builder → @summary-report-generator
    └─ @images-builder → @images-report-generator

T16: 【等待内容层完成】

T17-T18: 【第三层：组装层】
    └─ @report-assembler → 最终报告

T19: 完成
```

### 并行效率分析

```
假设单个任务耗时：
- Builder: 30秒
- Generator: 10秒

串行执行时间：
= 3任务 × (30 + 10) = 120秒

并行执行时间（分析层）：
= max(40, 40, 40) = 40秒

性能提升：3倍
```

---

## 数据流依赖

### 数据库表结构

#### news 表

```sql
CREATE TABLE news (
    id INTEGER PRIMARY KEY,
    url TEXT,
    title TEXT,
    content TEXT,
    image_urls TEXT,  -- JSON数组
    event_name TEXT,
    session_id TEXT,
    created_at TIMESTAMP
)
```

#### report_sections 表

```sql
CREATE TABLE report_sections (
    id INTEGER PRIMARY KEY,
    section_type TEXT,  -- validation/timeline/prediction/news/summary/images
    session_id TEXT,
    event_name TEXT,
    content_data TEXT,  -- JSON格式
    status TEXT,  -- completed/failed
    created_at TIMESTAMP
)
```

### 数据依赖图

```
news 表（原始数据）
    │
    ├──────────────────────────────┐
    │                              │
    ↓                              ↓
@news-builder                 @summary-builder
(步骤1)                         (步骤2A)
    │                              │
    ↓                              ↓
section_type="news"           section_type="summary"
    │                              │
    ↓                              │
@news-report-generator           │
(步骤1)                         │
    │                              │
    ↓                              │
.parts/02-news.md                 │
    │                              │
    ├──────────────┐               │
    │              │               │
    ↓              ↓               ↓
@images-    @summary-   @summary-
builder     report-     generator
(步骤2B)     generator
    │              │
    ↓              ↓
section_type=  .parts/01-
"images"        summary.md
    │
    ↓
@images-
report-
generator
    │
    ↓
.parts/06-images.md
    │
    └──────────────┐
                   │
                   ↓
            @report-assembler
```

---

## 需要新增和修改的智能体

### 1. @news-builder（需扩展）⭐

**当前状态**：✅ 已存在，需扩展功能

**文件**：`prompts/news-collector.md`

**扩展职责**：

1. **从 news 表读取已有新闻**
2. **自主探索收集新闻** ⭐ 新增
   - 与分析层的 builder 一致
   - 根据事件名称搜索相关新闻
   - 并行调用 @news-processor 处理单条新闻
3. **筛选新闻**
   - 分类为今日新闻 / 相关新闻
   - 去重、排序、过滤低质量新闻
4. **保存到数据库**
   - section_type="news"
   - 包含完整的新闻数据和 image_urls

**工作流程**：

```
1. 从 news 表读取已有新闻
2. 搜索补充新闻（web-browser_multi_search_tool）
3. 并行调用 @news-processor 处理每条新闻
4. 合并已有新闻 + 新收集新闻
5. 去重、筛选、排序
6. 保存到 report_sections 表
   section_type="news"
```

**关键点**：

- ⭐ **新增搜索能力**：不仅要读取已有新闻，还要主动搜索
- ⭐ **并行处理**：使用 @news-processor 并行处理搜索结果
- ⭐ **智能筛选**：去重、过滤低质量新闻、按重要性排序

---

### 2. @summary-builder（新增）⭐

**文件**：`prompts/summary-builder.md`

**职责**：

- ⚠️ **不进行网络搜索或页面访问**
- 直接读取数据库中已有的新闻数据
- 分析新闻内容，提取核心信息
- 生成2-3句话摘要
- 保存到数据库（section_type="summary"）

**工作流程**：

```
1. 使用 news-storage_search 读取事件新闻
2. 分析新闻标题、摘要、关键词
3. 提取核心信息（时间、地点、人物、事件）
4. 生成简洁摘要（150-200字）
5. 保存到 report_sections 表
   section_type="summary"
```

**输出格式**：

```json
{
  "summary": "事件摘要内容...",
  "key_points": ["关键点1", "关键点2"],
  "word_count": 150
}
```

**工具权限**：

- `news-storage_search` - 读取新闻
- `news-storage_save_report_section` - 保存摘要
- ❌ **不需要**：web-browser、downloader 等网络工具

---

### 3. @images-builder（新增）⭐

**文件**：`prompts/images-builder.md`

**职责**：

- ⚠️ **不进行网络搜索或页面访问**
- 直接读取数据库中已有的新闻数据
- 读取 section_type="news" 中的 image_urls
- **筛选图片**：选出有效的、相关性高的图片（限制数量，如5-10张）
- 下载图片到本地
- 保存图片元数据到数据库（section_type="images"）

**工作流程**：

```
1. 使用 news-storage_get_report_section 读取
   section_type="news"
2. 提取 image_urls 字段
3. 筛选图片：
   - 去重
   - 过滤无效URL
   - 优先选择来自权威媒体的图片
   - 限制数量（5-10张，避免过多）
4. 创建图片文件夹 {event_name}/
5. 使用 downloader_download_files 下载图片
6. 生成图片元数据
7. 保存到 report_sections 表
   section_type="images"
```

**输出格式**：

```json
{
  "images": [
    {
      "filename": "image1.jpg",
      "original_url": "https://...",
      "local_path": "./{event_name}/image1.jpg",
      "source_news": "新闻标题",
      "size": 102400,
      "downloaded": true,
      "relevance": "高"
    }
  ],
  "total_count": 6,
  "success_count": 6,
  "failed_count": 0,
  "skipped_count": 15
}
```

**筛选策略**：

- 优先选择来自权威媒体的图片
- 过滤掉明显不相关的图片
- 限制数量（避免过多影响加载速度）
- 跳过重复URL

**工具权限**：

- `news-storage_get_report_section` - 读取news数据
- `downloader_download_files` - 下载图片
- `bash` - 创建文件夹
- `news-storage_save_report_section` - 保存图片元数据
- ❌ **不需要**：web-browser 等搜索工具

---

### 4. @report-assembler（重构）⭐

**当前状态**：✅ 已存在，需完全重构

**文件**：`prompts/report-assembler.md`

**重构职责**：

- ⚠️ **不再调用各个 generator**
- ⚠️ **不再读取数据库**
- **直接收集 .parts 文件夹中的所有 md 文件**
- **合并为最终报告**
- **修正文件目录结构和路径错误**
- **修正内容中的结构错误**
- **清理临时文件**

**工作流程**：

```
1. 扫描 .parts 文件夹
2. 检查哪些部分文件已生成
3. 按顺序读取并合并：
   01-summary.md
   02-news.md
   03-validation.md
   04-timeline.md
   05-prediction.md
   06-images.md
4. 修正路径错误：
   - 图片路径使用相对路径
   - 文件链接使用正确格式
5. 修正结构错误：
   - 补充缺失的标题
   - 统一分隔线格式
   - 修复 Markdown 语法错误
6. 添加报告头部和尾部
7. 生成最终报告 {event_name}.md
8. 清理临时文件（可选）：
   - 删除 .parts 文件夹
   - 或保留 {event_name}/ 文件夹（包含 images）
```

**工具权限**：

- `read` - 读取文件
- `write` - 写入最终报告
- `bash` - 文件操作、清理临时文件
- ❌ **不需要**：news-storage、Task 工具

**修正示例**：

```python
# 修正图片路径
"图片路径：/output/report_xxx/事件/image.jpg"
↓
"图片路径：./事件/image.jpg"

# 修正文件链接
"详见 [02-news.md]"
↓
"详见 [新闻来源](#新闻来源)"
```

---

## 需要修改的其他文件

### 1. prompts/event-processor.md

**修改内容**：

- 实现三层架构调用
- 第一层：并行启动分析层（@validator-builder、@timeline-builder、@predictor-builder）
- 等待分析层完成
- 第二层-步骤1：启动 @news-builder → @news-report-generator
- 第二层-步骤2：并行启动 @summary-builder + @images-builder
- 等待内容层完成
- 第三层：启动 @report-assembler（纯文件组装）

### 2. prompts/images-report-generator.md

**修改内容**：

- 改为从数据库读取（section_type="images"）
- 移除下载图片逻辑（由 @images-builder 负责）
- 只负责生成 markdown 文件

### 3. prompts/news-collector.md

**修改内容**：

- 扩展搜索能力（主动搜索新闻）
- 并行调用 @news-processor 处理搜索结果
- 智能筛选：去重、过滤、排序

### 4. opencode.json

**修改内容**：

- 添加 @summary-builder 配置（新增）
- 添加 @images-builder 配置（新增）
- 更新 @news-builder 权限（添加搜索和Task工具）
- 更新 @event-processor 权限（添加调用新智能体的权限）
- 更新 @images-report-generator 权限（移除下载工具）
- 更新 @report-assembler 权限（移除数据库和Task工具）

---

## 错误处理机制

### 容错策略

#### 1. Builder 层失败

**行为**：

- 标记 section 状态为 "failed"
- 记录 error_message

**后续处理**：

- Generator 层检测到 failed 状态
- 跳过生成，返回错误状态
- 最终报告中标注该部分缺失

#### 2. Generator 层失败

**行为**：

- 记录错误日志
- 不影响其他部分生成

**后续处理**：

- @report-assembler 检测到文件缺失
- 在最终报告中标注该部分生成失败

#### 3. 图片下载失败

**行为**：

- @images-builder 标记失败的图片
- 继续处理其他图片
- 保存部分成功的数据

**后续处理**：

- @images-report-generator 生成文件时标注"部分图片下载失败"
- 不影响整体报告生成

### 错误报告格式

```markdown
## 生成状态说明

- ✅ 01-事件摘要：已完成
- ✅ 02-新闻来源：已完成
- ✅ 03-真实性验证：已完成
- ✅ 04-事件时间轴：已完成
- ⚠️ 05-趋势预测：生成失败（数据不足）
- ⚠️ 06-相关图片：部分缺失（3/10图片下载失败）
```

---

## 文件输出清单

### 临时文件（.parts/）

```
.parts/
├── 01-summary.md      ← 第二层·步骤2 生成
├── 02-news.md         ← 第二层·步骤1 生成
├── 03-validation.md   ← 第一层·任务1 生成
├── 04-timeline.md     ← 第一层·任务2 生成
├── 05-prediction.md   ← 第一层·任务3 生成
└── 06-images.md       ← 第二层·步骤2 生成
```

### 最终文件

```
{event_name}.md         ← 第三层组装生成
```

### 图片文件夹

```
{event_name}/
├── image1.jpg
├── image2.png
└── ...
```

---

## 实施计划

### Phase 1: 新增智能体（P0）

- [ ] 创建 `prompts/summary-builder.md`
- [ ] 创建 `prompts/images-builder.md`
- [ ] 在 `opencode.json` 中添加配置

### Phase 2: 修改现有智能体（P0）

- [ ] 修改 `prompts/event-processor.md`
- [ ] 修改 `prompts/images-report-generator.md`
- [ ] 修改 `prompts/report-assembler.md`

### Phase 3: 更新配置（P0）

- [ ] 更新 `opencode.json` 权限配置
- [ ] 更新 `WORKFLOW.md` 文档

### Phase 4: 测试验证（P1）

- [ ] 单元测试：新增智能体
- [ ] 集成测试：完整流程
- [ ] 边界测试：错误处理

### Phase 5: 文档更新（P2）

- [ ] 更新 README.md
- [ ] 更新 API 文档
- [ ] 更新架构图

---

## 关键注意事项

### ⚠️ 数据一致性

1. **session_id 传递**：确保所有智能体使用相同的 session_id
2. **event_name 传递**：确保文件路径和数据库查询使用相同的事件名称
3. **section_type 命名**：严格按照约定命名（validation/timeline/prediction/news/summary/images）

### ⚠️ 文件路径

1. **相对路径**：图片使用相对路径 `./{event_name}/image.jpg`
2. **临时文件**：统一放在 `.parts/` 文件夹
3. **最终文件**：与 .parts/ 同级目录

### ⚠️ 并发安全

1. **文件夹创建**：使用 bash mkdir -p 确保文件夹存在
2. **文件写入**：使用 write 工具的 atomic 写入
3. **数据库操作**：使用事务保证数据一致性

---

## 性能指标

### 预期性能提升

| 阶段         | 旧架构         | 新架构    | 提升       |
| ------------ | -------------- | --------- | ---------- |
| 分析层       | 串行 120秒     | 并行 40秒 | 3x         |
| 内容层-步骤1 | 并行（有问题） | 串行 20秒 | 保证正确性 |
| 内容层-步骤2 | 并行（有问题） | 并行 20秒 | 保证正确性 |
| 总耗时       | ~180秒         | ~80秒     | 2.25x      |

### 资源消耗

| 资源       | 旧架构 | 新架构 |
| ---------- | ------ | ------ |
| 数据库读写 | 高     | 中     |
| 磁盘 I/O   | 高     | 中     |
| 网络请求   | 高     | 中     |
| 内存占用   | 高     | 低     |

---

## 版本历史

| 版本 | 日期       | 变更内容                               | 作者          |
| ---- | ---------- | -------------------------------------- | ------------- |
| v2.0 | 2026-01-31 | 新架构设计，三层架构，解决图片缺失问题 | NewsAgent团队 |
| v1.0 | 2026-01-30 | 初始版本，两层架构                     | NewsAgent团队 |

---

## 附录

### A. 相关文档

- [WORKFLOW.md](../WORKFLOW.md) - 完整业务流程文档
- [opencode.json](../opencode.json) - 智能体配置文件

### B. 模板文件

- `templates/sections/summary-section-template.md`
- `templates/sections/news-section-template.md`
- `templates/sections/validation-section-template.md`
- `templates/sections/timeline-section-template.md`
- `templates/sections/prediction-section-template.md`
- `templates/sections/images-section-template.md`

### C. 数据库Schema

- `data/news_storage.db` - 主数据库文件

---

**文档结束**
